package org.client;

import javax.swing.*;
import java.awt.*;

public class CalenderFrame extends JFrame {
    public CalenderFrame(){ //Sets the MainFrame for the GUI
        setTitle("Calender GUI");
        setSize(600, 280);
        setResizable(false);
        setLayout(new BorderLayout());

    }
}